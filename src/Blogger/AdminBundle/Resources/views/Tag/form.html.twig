<form action="{% if create  %}{{ path('BloggerAdminBundle_tag_create') }}{% else %}{{ path('BloggerAdminBundle_tag_submitEdition', { 'tagId': tag.id }) }}
                {% endif %}"
method="post" {{ form_enctype(form) }} class="blogger">

    {{ form_widget(form) }}
    {{ form_rest(form) }}
    <p>
        <input type="submit" value="Save">
    </p>
</form>